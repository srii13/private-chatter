{
  "name": "private-chatter-deploy",
  "version": "1.0.0",
  "description": "Root package for deploying the unified backend and frontend to Render",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client --legacy-peer-deps && npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
